[core]
	engine = pg
	top_dir = sqitch

[deploy]
	verify = true

# Lancer les migrations avec le rôle migrator (DDL) :
#   sqitch deploy db:pg://$MIGRATOR_DATABASE_URL
# Ne jamais utiliser secondmind_app (DML only) pour les migrations.
[engine "pg"]
	registry = sqitch
	client = psql
