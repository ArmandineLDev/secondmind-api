meta {
  name: Modifier un SWOT
  type: http
  seq: 9
}

put {
  url: {{apiUrl}}/swots/{{swotId}}
  body: json
  auth: none
}

body:json {
  {
    "threats": "Concurrents installés, dépendance à un hébergeur unique"
  }
}

assert {
  res.status: eq 200
}
