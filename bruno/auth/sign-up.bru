meta {
  name: Sign Up (fermé)
  type: http
  seq: 90
}

post {
  url: {{baseUrl}}/api/auth/sign-up/email
  body: json
  auth: none
}

headers {
  Origin: {{baseUrl}}
}

body:json {
  {
    "name": "Compte Test",
    "email": "nouveau@exemple.dev",
    "password": "motdepasse-long-10"
  }
}

docs {
  ⚠️ L'inscription publique est fermée (disableSignUp: true dans lib/auth.ts).
  Cette requête doit donc ÉCHOUER — elle est conservée pour vérifier que la
  fermeture est bien effective. Un 200 ici serait une régression de sécurité.
}
