meta {
  name: Détail d'une entreprise
  type: http
  seq: 3
}

get {
  url: {{apiUrl}}/companies/{{companyId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{companyId}}
}
