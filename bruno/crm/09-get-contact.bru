meta {
  name: Détail d'un contact
  type: http
  seq: 9
}

get {
  url: {{apiUrl}}/contacts/{{contactId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{contactId}}
}
