meta {
  name: Archiver un projet
  type: http
  seq: 5
}

patch {
  url: {{apiUrl}}/projects/{{projectId}}/archive
  body: json
  auth: none
}

body:json {
  {
    "archive": true
  }
}

assert {
  res.status: eq 200
  res.body.is_archived: eq true
}
