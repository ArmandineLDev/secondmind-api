meta {
  name: Lister l'inbox
  type: http
  seq: 2
}

get {
  url: {{apiUrl}}/tasks?stage=inbox
  body: none
  auth: none
}

params:query {
  stage: inbox
}

assert {
  res.status: eq 200
  res.body: isArray
}
