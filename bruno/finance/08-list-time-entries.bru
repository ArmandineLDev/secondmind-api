meta {
  name: Lister les saisies de temps
  type: http
  seq: 8
}

get {
  url: {{apiUrl}}/time-entries?project_id={{finProjectId}}
  body: none
  auth: none
}

params:query {
  project_id: {{finProjectId}}
  ~year: 2026
  ~month: 8
}

assert {
  res.status: eq 200
  res.body: isArray
}
