meta {
  name: Setup — projet support
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/projects
  body: json
  auth: none
}

body:json {
  {
    "name": "[Bruno] Projet support finance"
  }
}

vars:post-response {
  finProjectId: res.body.id
}

assert {
  res.status: eq 201
}
